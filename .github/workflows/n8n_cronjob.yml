name: Ping URL every 15 min

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:   # allows manual trigger (important for activation)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: |
          curl -I https://n8n-sever-umar.onrender.com
