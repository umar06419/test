name: Ping URL every 15 min

on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -I https://n8n-sever-umar.onrender.com
